import React from "react";
import ReactDOM from "react-dom/client";
import PartiyaApp from "./PartiyaApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PartiyaApp />
  </React.StrictMode>
);
